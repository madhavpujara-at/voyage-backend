import { AnalyticsData } from "../../../domain/types/AnalyticsData";

/**
 * Response DTO for the GetAnalyticsData use case
 * This is the same as the AnalyticsData domain type
 */
export type GetAnalyticsDataResponseDto = AnalyticsData;
